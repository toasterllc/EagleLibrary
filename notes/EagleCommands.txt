* Move selection by delta (x,y)
MOVE (>0 0) (x y);

* Undo ratsnest
ripup @;

* Generate netlist
    - Open layout
    - Execute `run export-ict-netlist-pad-coordinates`

* Automate pin creation
    - Use an SCR script:

      pin short both io R0 'VCC'      (0 -00.0);
      pin short both io R0 'VCCIO'    (0 -00.1);
      ...
